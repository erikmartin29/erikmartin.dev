{
  "name": "erikmartin-dev",
  "private": true,
  "version": "1.0.0",
  "description": "erikmartin.dev -- personal blog/portfolio",
  "scripts": {
    "dev": "pnpm run --parallel dev",
    "build": "pnpm run --parallel build"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=10.0.0"
  }
}